#include "Plateau.h"

Plateau::Plateau()
{
    /*for (int i = 0; i < ligne; i++)
    {
        vector<int> ligne;
        for (int j = 0; j < colonne; j++)
        {
            ligne.push_back(colonne); // Add an element (column) to the row
        }
        tab.push_back(ligne); // Add the row to the main vector
    }*/
    /*int ** tab= new int*[ligne];
    for (int i=0; i <ligne; i++)
    {
        tab[i]= new int [colonne];
    }*/


}

Plateau::~Plateau()
{

    /*for (int i=0; i < LIGNE; i++)
    {
        delete[] tab[i];
    }
    delete[] tab ;*/
}

